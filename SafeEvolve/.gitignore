.venv/
__pycache__/
*.pyc
.DS_Store
.ipynb_checkpoints/
# comparison.png is committed (the README embeds it); the CSV audit logs are
# regenerated on every run, so keep them out of diffs.
results/*.csv
